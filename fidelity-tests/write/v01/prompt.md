# gdt-write-v01 — prompt

No builder agent. The fixture is produced by `build.sh`; the only feature it needs is a
bulleted terminal paragraph, which the Docs API can create (`createParagraphBullets` on the
tab's last, empty paragraph).
